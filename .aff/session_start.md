Use `mise tasks ls` to discover workflows and `mise run <task>` for repo logic. Do not wrap `mise`, and avoid raw `cargo` or `xcodebuild`. Stay read-only outside the working tree unless the user explicitly approves writes. When working directly in local `main`, do not rebase, amend, or force-push. Assign one spawned worktree and one reused lane to the whole agent session, not to each task. Reuse that same build/test/runtime lane for the whole session so caches stay warm and avoid cold rebuilds. After every commit in that session worktree, rebase the worktree branch onto current local `main`, resolve conflicts in the worktree first, make sure the change builds or passes the smallest relevant validation in the worktree, and replay only the finished task commit into `main`. Do not replay dirty files. Do not clean up the session worktree or lane after each task; keep them until the session ends or the user explicitly asks for cleanup. This is a hard rule. In the assigned worktree, rebasing onto local `main` and amending only your own unpublished commits are allowed when needed to keep the branch replayable; never rewrite local `main` history or force-push shared branches. Commits must use path-limited `git commit -sS -- <paths>`; for brand-new files run `git add -N -- <new-paths>` first; never use plain `git add`, `git add -A`/`.`, `git commit -a`, or `git commit -i` — parallel agents may have unrelated edits staged; verify `Signed-off-by: Bart Smykla <bartek@smykla.com>`. Run the relevant gate before committing unless the change is docs, version-sync, or tiny noise. Evaluate semver on each change, but only bump versions with explicit user approval. Small changes can skip a bump unless shipped `harness` or `aff` logic changes enough that the local binary must be reinstalled. Investigate call sites before fixing. Work in small chunks: test, implement, verify. Keep Rust files under 520 lines and functions under 100 lines. If blocked by another agent for 5 minutes, ask the user. Hard stop if 1Password is unavailable. For shared `apps/harness-monitor` Xcode and XcodeBuildMCP work, use `mise run monitor:agent:*`.
