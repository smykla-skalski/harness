Use `mise tasks ls` to discover workflows; run repo logic via `mise run <task>`; do not wrap `mise` or use raw `cargo`/`xcodebuild`. Stay read-only outside the worktree unless the user explicitly approves writes. On local `main`, never rebase, amend, or force-push. Use one spawned worktree plus one reused build/test/runtime lane for the whole agent session, not per task, so caches stay warm. After each worktree commit, rebase onto current local `main`, resolve conflicts in the worktree, make sure the change builds or passes the smallest relevant validation there only for affected surfaces, and replay only the finished task commit into `main`; never replay dirty files. Helper scripts, docs, and files outside an app/codebase do not require app builds or unrelated gates. After replay onto `main`, do not rerun builds or checks there just because of the replay; continue unless the user asks for more validation or a new affected surface appears. Keep the session worktree and lane until session end or explicit cleanup request; this is a hard rule. In the assigned worktree, rebasing onto local `main` and amending only your own unpublished commits are allowed when needed to keep the branch replayable; never rewrite local `main` history or force-push shared branches. Commit path-limited with `git commit -sS -- <paths>`; for brand-new files run `git add -N -- <new-paths>` first; never use plain `git add`, `git add -A`/`.`, `git commit -a`, or `git commit -i` — parallel agents may have unrelated edits staged; verify `Signed-off-by: Bart Smykla <bartek@smykla.com>`. Run the relevant gate before committing unless the change is docs, version-sync, tiny noise, or otherwise outside the affected codebase. Evaluate semver on each change, but bump versions only with explicit user approval; small changes can skip a bump unless shipped `harness` or `aff` logic changed enough that the local binary must be reinstalled. Investigate call sites before fixing. Work in small chunks: test, implement, verify. Keep Rust files under 520 lines and functions under 100 lines. If blocked by another agent for 5 minutes, ask the user. Hard stop if 1Password is unavailable. For shared `apps/harness-monitor` Xcode and XcodeBuildMCP work, use `mise run monitor:*`.
