Read the nearest applicable `AGENTS.md` first and treat it as canonical. Before creating an editing worktree, select `pr` (default) or user-confirmed `replay`, and record the mode in substantial plans and handoffs. Use one session worktree and one reused build/test/runtime lane; run repo logic through discovered `mise run <task>` workflows, with direct Cargo allowed only for targeted Rust diagnosis that has no equivalent `mise` task. Commit only explicit paths with `git commit -sS -- <paths>`, use `git add -N -- <new-paths>` only for new files, verify the exact Bart Smykla sign-off, validate only affected surfaces, deliver only clean committed state, and change versions only with explicit approval. Before integration or publication, read `docs/agent-guides/delivery-workflows.md` and stop if it is unavailable.
