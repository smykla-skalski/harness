// Command handler integration tests.
// Split by topic: schema, context, session_stop, setup_gateway.

mod context;
mod observe;
mod schema;
mod session;
mod session_stop;
mod setup_gateway;
mod signal;
